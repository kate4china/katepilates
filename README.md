# Kate Pilates

Fresh static implementation from the new Pilates mockup.

## Assets

- `assets/pilates-hero.png` was generated with the built-in image generation workflow for this project.
- `assets/pilates-logo-lockup.png` is the large hero logo lockup above the main headline.
- `assets/header-logo-cropped.png` is the header logo mark, with the "Kate Pilates" wordmark rendered as HTML text. Tab icon uses `assets/logo.svg`. `assets/icon-*.png` are benefit icons.
